/**
 * AdminUser
 *
 * @typedef {Object} AdminUser
 * @property {number} id
 * @property {string} username
 * @property {string} password_hash   - bcrypt hash, never returned to clients
 * @property {string} created_at
 */

module.exports = {};
